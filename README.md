# BERT Sentiment Analysis Tool 🤖

A Python-based tool that uses a pre-trained **BERT (Bidirectional Encoder Representations from Transformers)** model to analyze the sentiment of product reviews.

## 🚀 Features
* Uses the `nlptown/bert-base-multilingual-uncased-sentiment` model.
* Processes batch data from a `data.csv` file.
* Outputs a new CSV with sentiment labels (1-5 stars) and confidence scores.

## 🛠️ Setup & Installation
1. **Clone the repository:**
   ```bash
   git clone [https://github.com/RishanaAbubacker/bert-sentiment-analysis.git](https://github.com/RishanaAbubacker/bert-sentiment-analysis.git)
   cd bert-sentiment-analysis